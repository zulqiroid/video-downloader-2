package com.core.ads.domain.placement

/**
 * Used for analytics, logging, debugging, and policy evaluation.
 */
enum class AdFormat {
    APP_OPEN,
    INTERSTITIAL,
    REWARDED,
    BANNER,
    NATIVE
}